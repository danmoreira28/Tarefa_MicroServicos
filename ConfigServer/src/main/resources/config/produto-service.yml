server:
  port: 8082
  wait-time-in-ms-when-sync-empty: 3000

spring:
  spring:
    profiles:
      active: native
  data:
      mongodb:
        uri: mongodb://admin:admin@127.0.0.1:27017/microservicos
        database: produto
        auto-index-creation: true

application-version: 1.0
